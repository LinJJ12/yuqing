# `frontend/src/assets/` — 静态资源

上级规范：[../README.md](../README.md)

## 职责

- 图标、图片等静态文件（含品牌 `logo.png`，侧栏导入）。
- 公开 favicon / 直链副本在 `frontend/public/logo.png`。
- 全局样式目前在 `frontend/src/style.css`（由 `main.js` 引入），不强制放在本目录。

## 禁止

- 存放后端数据导出、模型文件
- 用巨型未压缩素材撑仓库
